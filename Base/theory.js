// =========== Operators
let a = 10
let b = 5
let c = 32

// c = c + a
// c = c - a
// c = c * a
// c = c / a
//c += a
//c  = 4 ** 2

// ========== Data types
const age = 29 // Number
const name = 'Andrew' // String
const isProgrammer = true // boolean
let x // undefined - нет значения, но существует
// console.log(typeof null) // typeof - определение типа данных
// null - пустой, отдельный тип данных
// console.log(typeof (1 / x)) // Определяет, что это число
// console.log(typeof NaN)

// ============= Priority
const fullAge = 29
const birthYear = 1993
const currentYear = 2023
// > < >= <=
const isFullAge = currentYear - birthYear >= fullAge

// console.log(isFullAge)

const num1 = 42
const num2 = '42'
console.log(num1 === num2)